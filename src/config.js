// 配置
const host = 'https://www.famulei.com' // 生产
const apiHost = 'https://api-v2.famulei.com' // 生产
// const host = 'https://staging-1.famulei.com' // 测试
// const apiHost = 'https://staging-api-1.famulei.com' // 测试
const config = {
  host,
  apiHost
}

export default config
